@using Fashion.DAL.Models
@model Product

<div class="card-body">
    <h4 class="card-title">Order Status</h4>
    <div class="table-responsive">
        <table class="table">
            <thead>

                <tr>

                    <th> Id </th>
                    <th> ImgUrl </th>
                    <th> Name </th>
                    <th> ShortDescription </th>
                    <th> Price </th>

                    <th> elave </th>
                </tr>
            </thead>
            <tbody>


                @if (Model is not null)
                 
                 {       <tr>


                            <td> @Model.Id </td>
                            <td> <img src="~/assets/UploadedImages/@Model.Imgurl">  </td>
                            <td> @Model.Name </td>
                            <td> @Model.ShortDescription </td>
                            <td> @Model.Price </td>
                            <td>
                                <form asp-action="Delete" asp-controller="Product" method="post" onsubmit="return confirm('Are you sure you want to delete this item?');">
                                    <input type="hidden" name="id" value="@Model.Id" />
                                    <button type="submit" class="btn btn-danger btn-rounded btn-fw">
                                        Delete
                                    </button>Model
                                </form>                     
                                <a asp-area="Admin" asp-controller="Product" asp-action="Update" asp-route-id="@Model.Id" class="btn btn-light m-2">Update</a>

                            </td>
                        </tr>


                        
                    
}




            </tbody>
        </table>
    </div>
</div>


